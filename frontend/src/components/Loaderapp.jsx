import Loader from "@/ui/loader";

export default function Loaderapp(){
    return(
        <div className="items-center justify-center flex h-screen bg-black">
            <Loader/>
        </div>
    )
}